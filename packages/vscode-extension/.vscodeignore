# Whitelist approach: exclude everything, then re-include only what belongs in the VSIX.
# Required for monorepos — vsce traverses workspace symlinks and parent directories,
# pulling in .git/, core source, root config files, and other junk without this.

**

# Bundled extension runtime
!out/extension.js
!out/sql-wasm.wasm

# Marketplace metadata
!package.json
!README.md
!CHANGELOG.md
!LICENSE

# Icon
!assets/
!assets/blue-matter.png
