# Optional: set in environment instead of VS Code settings
# Open Router (default, free): https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# Anthropic (set Untitled: API Provider to "anthropic" in settings)
# ANTHROPIC_API_KEY=sk-ant-...
